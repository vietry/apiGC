{
  "name": "apigc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tsnd --respawn --clear src/app.ts",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/app.js",
    "prisma:sqlserver:test": "dotenv -e .env.test -- npx prisma migrate deploy",
    "test": "npm run prisma:sqlserver:test && jest",
    "test:watch": "npm run prisma:sqlserver:test && jest --watch",
    "test:coverage": "npm run prisma:sqlserver:test && jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/compression": "^1.7.5",
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.5",
    "@types/supertest": "^6.0.2",
    "dotenv-cli": "^7.4.1",
    "jest": "^29.7.0",
    "prisma": "^5.13.0",
    "rimraf": "^5.0.5",
    "supertest": "^6.3.4",
    "ts-jest": "^29.1.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@prisma/client": "^5.13.0",
    "compression": "^1.7.4",
    "dotenv": "^16.3.1",
    "env-var": "^7.4.1",
    "express": "^4.18.2"
  }
}
